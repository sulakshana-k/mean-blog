import { ElseDirective } from './else.directive';

describe('ElseDirective', () => {
  it('should create an instance', () => {
    const directive = new ElseDirective();
    expect(directive).toBeTruthy();
  });
});
